---
description: Morya 设计系统第一信源。生成或修改 UI 前必读 DESIGN.md 与 design-tokens。
alwaysApply: true
---

# Morya 设计系统

1. **先读** 项目根目录 `DESIGN.md`，再读 `docs/components.md`。
2. **只用** `morya-ui` 的 `M*` 组件；全局样式 `morya-ui/styles.css`。
3. **颜色/间距/圆角** 使用 `--m-*` CSS 变量，禁止裸 hex/rgb（见 `scripts/check-raw-colors.mjs`）。
4. **表单选项** 用 `MSelect` / `MTreeSelect`；**动作菜单** 用 `MDropdown`。
5. 不确定 API 时查 morya-ui 文档或 MCP，**禁止臆造 prop**。
6. 根节点用 `MConfigProvider`；后台布局用 `MLayout` 系列。

Token 参考：`design-tokens/tokens.json`
